---
# Source: loki/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: logs-loki
  namespace: logs
  labels:
    app: loki
    chart: loki-0.30.1
    release: logs
    heritage: Tiller
  annotations:
    {}
    
spec:
  type: ClusterIP
  ports:
    - port: 3100
      protocol: TCP
      name: http-metrics
      targetPort: http-metrics
  selector:
    app: loki
    release: logs
