---
# Source: promtail/templates/clusterrolebinding.yaml

kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: logs-promtail-clusterrolebinding
  labels:
    app: promtail
    chart: promtail-0.8.0
    release: logs
    heritage: Tiller
subjects:
  - kind: ServiceAccount
    name: logs-promtail
    namespace: default
roleRef:
  kind: ClusterRole
  name: logs-promtail-clusterrole
  apiGroup: rbac.authorization.k8s.io
